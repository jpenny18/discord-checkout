export interface UserData {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  country: string;
  selectedType?: string;
  accountSize?: string;
  platform?: string;
  timestamp?: Date;
  status?: string;
} 