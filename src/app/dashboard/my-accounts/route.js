export const dynamic = 'force-dynamic';
export const fetchCache = 'force-no-store';
export const revalidate = 0;

// This file's only purpose is to ensure Next.js treats this route as fully dynamic
// and doesn't attempt to statically generate it during build time. 